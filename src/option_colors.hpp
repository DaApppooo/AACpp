#include "settings.hpp"

namespace background_color
{
  void init(Option&);
  void mainbox(Option&, Rectangle);
  Rectangle popup(Option&);
  void destroy(Option&);
}
namespace text_color
{
  void init(Option&);
  void mainbox(Option&, Rectangle);
  Rectangle popup(Option&);
  void destroy(Option&);
}
namespace text_field_background_color
{
  void init(Option&);
  void mainbox(Option&, Rectangle);
  Rectangle popup(Option&);
  void destroy(Option&);
}
namespace command_background_color
{
  void init(Option&);
  void mainbox(Option&, Rectangle);
  Rectangle popup(Option&);
  void destroy(Option&);
}

